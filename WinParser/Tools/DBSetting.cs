using System;
using System.Collections.Generic;
using System.Text;

namespace WinParser.Tools
{
    public static class DBSetting
    {
        /*public static void SetSetting(string serverName, string dbName, string userName, string password)
        {
            Properties.Settings.Default.ServerName = serverName;
            Properties.Settings.Default.DbName = dbName;
            Properties.Settings.Default.UserName = userName;
            Properties.Settings.Default.Password = password;

            Properties.Settings.Default.Save();
        }*/
    }
}
